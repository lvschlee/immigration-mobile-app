import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    marginTop: 15,
    borderWidth: 1,
    borderColor: '#d1d5db',
    borderRadius: 3,
  },
  input: {
    width: '100%',
    height: 50,
  },
});
